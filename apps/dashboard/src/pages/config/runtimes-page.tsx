export function RuntimesPage(): JSX.Element {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold mb-4">Runtimes</h1>
      <p className="text-muted-foreground">
        Configure runtime environments for task execution. Manage container
        images, resource limits, environment variables, and execution
        parameters.
      </p>
    </div>
  );
}
