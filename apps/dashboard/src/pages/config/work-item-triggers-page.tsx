import { ConfigPlaceholderPage } from './config-placeholder-page.js';

export function WorkItemTriggersPage(): JSX.Element {
  return (
    <ConfigPlaceholderPage
      title="Triggers"
      description="Future trigger management will return here after the current automation system is removed."
    />
  );
}
