import { describe, it, expect } from 'vitest';
import { CommandCenterDrawer } from './command-center-drawer.js';

describe('CommandCenterDrawer', () => {
  it('exports CommandCenterDrawer', () => expect(typeof CommandCenterDrawer).toBe('function'));
});
