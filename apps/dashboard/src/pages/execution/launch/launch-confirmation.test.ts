import { describe, expect, it } from 'vitest';

import { LaunchConfirmation } from './launch-confirmation.js';

describe('LaunchConfirmation', () => {
  it('exports LaunchConfirmation', () => expect(typeof LaunchConfirmation).toBe('function'));
});
