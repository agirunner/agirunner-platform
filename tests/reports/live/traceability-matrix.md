# AgentBaton Platform v1.0 — Live Test Traceability Matrix (Generated)

This matrix is now generated under `tests/reports/live/`.

Source-of-truth inputs:
- `tests/reports/live/traceability.state.json`
- `tests/reports/live/run-*.json`

Human-readable run summaries:
- `tests/reports/live/run-*.md`

Use the scripted flow to refresh this file:

```bash
pnpm test:traceability:run
```
