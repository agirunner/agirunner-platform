# Greeting CLI

This repository is the baseline seed for the `sdlc-review-rework-once` live workflow test.

## Usage

Run the CLI with:

```bash
sh greet.sh
```

## Tests

Run the regression check with:

```bash
make test
```
