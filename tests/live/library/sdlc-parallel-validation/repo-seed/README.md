# Greeting CLI

This repository is the baseline seed for the `sdlc-parallel-validation` live workflow test.

## Usage

Run the CLI with:

```bash
sh greet.sh
```

## Tests

Run the regression check with:

```bash
make test
```
