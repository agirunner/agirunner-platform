# Greeting CLI

This repository is the baseline seed for the `sdlc-qa-fails-once` live workflow test.

## Usage

Run the CLI with:

```bash
sh greet.sh
```

## Tests

Run the regression check with:

```bash
make test
```
