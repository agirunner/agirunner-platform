{
  "name": "Content Stale Assessment After Rework Pipeline",
  "slug": "live-test-content-stale-assessment-arrives-after-rework-v1",
  "description": "Revision-aware content workflow that rejects stale assessments from older revisions.",
  "outcome": "Only the active subject revision may clear the workflow after rework.",
  "lifecycle": "planned",
  "definition": {
    "process_instructions": "Run the workflow through the authored stages `concept-pass`, `draft-pass`, and `publish-pass`. Treat each stage as a distinct checkpoint of evidence and handoff quality: `concept-pass` exists to Design direction exists; `draft-pass` exists to Implementation revisions are assessed; `publish-pass` exists to Release packaging is validated.\nUse these roles deliberately: `stale-content-strategist` Produces the initial design and later remediation guidance when routed; `stale-content-author` Implements and reimplements the delivery subject over multiple revisions; `stale-quality-assessor` Assesses implementation revisions and release-readiness revisions; `stale-policy-assessor` Assesses downstream integration risk after implementation revisions; `stale-publication-lead` Packages the release candidate after assessments clear.\nHappy path: start with the earliest unfinished stage, dispatch the best matching producing role for that stage, require a full handoff with concrete artifacts or evidence, then move to the next stage only when the current output is usable. When a later stage is primarily evaluative, dispatch the reviewer or assessor roles after the producing role has handed off; when a later stage is packaging or release-oriented, dispatch the coordinating role only after upstream evidence is complete.\nUnhappy path: If any role reports missing evidence, partial delivery, request changes, rejection, blocking risk, or an escalation, keep the current subject open, route work back to the most relevant producing role, and require a fresh handoff before trying again. If two decisions conflict, preserve both in history, but route using the newest subject state and the most restrictive still-active finding. Do not skip a stage just because a later role believes it can improvise around incomplete upstream work. Late reviewer output from an older revision is evidence, not authority. The orchestrator SHOULD ignore stale decisions for routing, preserve them in history, and continue from the newest subject revision only.\nCompletion rules: finish only after the last required stage has a final, internally consistent handoff, the final package matches the current subject state, and every role named above has either contributed or been intentionally skipped for a clear reason grounded in the playbook. Stage-specific closure for this playbook: each `concept-pass` work item stays open until `stale-content-strategist` has either submitted current-subject handoffs that satisfy the `concept-pass` goal or been intentionally skipped with a concrete reason grounded in the playbook; each `draft-pass` work item stays open until `stale-content-author`, `stale-quality-assessor`, and `stale-policy-assessor` have either submitted current-subject handoffs that satisfy the `draft-pass` goal or been intentionally skipped with a concrete reason grounded in the playbook; each `publish-pass` work item stays open until `stale-publication-lead` and `stale-quality-assessor` have either submitted current-subject handoffs that satisfy the `publish-pass` goal or been intentionally skipped with a concrete reason grounded in the playbook. Do not fabricate approvals, assessments, escalations, or branch changes that the playbook does not actually justify, and do not treat stale findings as permission to advance new work. Guided closure rule: treat the authored flow as best-intent process guidance. Preferred review paths and stage order SHOULD be followed when they remain useful, but the orchestrator MAY waive a preferred step with a recorded reason when closure is still legally possible. Only explicit blockers grounded in the current subject state should keep work open; non-blocking deviations, redundant reviews, and stale advisory findings should be recorded as callouts instead of preventing closure.",
    "parameters": [
      {
        "slug": "goal",
        "title": "Goal",
        "required": true
      }
    ],
    "roles": [
      "stale-content-strategist",
      "stale-content-author",
      "stale-quality-assessor",
      "stale-policy-assessor",
      "stale-publication-lead"
    ],
    "board": {
      "entry_column_id": "planned",
      "columns": [
        {
          "id": "planned",
          "label": "Planned",
          "description": "Work is defined but not yet assigned."
        },
        {
          "id": "active",
          "label": "In Progress",
          "description": "A specialist is actively working."
        },
        {
          "id": "blocked",
          "label": "Blocked",
          "description": "Work is blocked and needs intervention.",
          "is_blocked": true
        },
        {
          "id": "done",
          "label": "Done",
          "description": "The work item meets its acceptance criteria.",
          "is_terminal": true
        }
      ]
    },
    "stages": [
      {
        "name": "concept-pass",
        "goal": "Design direction exists.",
        "involves": [
          "stale-content-strategist"
        ]
      },
      {
        "name": "draft-pass",
        "goal": "Implementation revisions are assessed.",
        "involves": [
          "stale-content-author",
          "stale-quality-assessor",
          "stale-policy-assessor"
        ]
      },
      {
        "name": "publish-pass",
        "goal": "Release packaging is validated.",
        "involves": [
          "stale-publication-lead",
          "stale-quality-assessor"
        ]
      }
    ],
    "lifecycle": "planned",
    "orchestrator": {
      "max_rework_iterations": 5,
      "max_active_tasks": 4,
      "max_active_tasks_per_work_item": 2,
      "allow_parallel_work_items": false
    }
  }
}
