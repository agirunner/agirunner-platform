# Greeting CLI

This repository is the baseline seed for the `bug-fix` live workflow test.

## Usage

Run the CLI with:

```bash
sh greet.sh [name]
```

The default greeting should remain `Hello, world!`.

## Tests

Run the regression check with:

```bash
make test
```
