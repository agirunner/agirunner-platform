{
  "name": "default-image-assessment-flow",
  "profile": "default-image-assessment-flow",
  "workflow": {
    "name": "Default Image Assessment Flow",
    "goal": "Verify that a generic assessment-driven repository workflow succeeds using only the tenant default execution environment path.",
    "parameters": {
      "scenario_name": "default-image-assessment-flow",
      "assessment_contract": "Approve the first implementation revision when the seeded verification path passes and the delivery output stays within the tenant default execution environment tool contract."
    }
  },
  "workspace": {
    "repo": true,
    "memory": {},
    "spec": {
      "instructions": {
        "content": "Use the default execution environment only. Keep the existing seeded stack and verify with ./scripts/verify.sh.",
        "format": "markdown"
      }
    }
  },
  "approvals": [],
  "actions": [],
  "expect": {
    "state": "completed",
    "outcome_envelope": {
      "allowed_states": [
        "completed"
      ],
      "require_output_artifacts": true,
      "require_completed_non_orchestrator_tasks": true,
      "require_terminal_work_items": true,
      "require_db_state": true,
      "require_runtime_cleanup": true,
      "require_fatal_log_free": true
    }
  },
  "coverage": {
    "storage_execution": [
      "default_execution_environment"
    ],
    "delivery_paths": [
      "assessment_then_successor"
    ]
  }
}
