{
  "name": "prose-only-approval-advisory",
  "profile": "prose-only-approval-advisory",
  "workflow": {
    "name": "Prose Only Approval Advisory",
    "goal": "Produce a release note packet where informal approval may be mentioned in prose but no configured gate exists.",
    "parameters": {
      "audience": "operators",
      "format": "release note"
    }
  },
  "workspace": {
    "repo": false,
    "memory": {
      "audience": "operators"
    },
    "spec": {
      "instructions": {
        "content": "Deliver a short release note packet. If the work looks good, it may mention that a human approval would be nice before release, but there is no configured approval gate and the workflow must still complete."
      }
    }
  },
  "approvals": [],
  "actions": [],
  "expect": {
    "state": "completed",
    "work_items": {
      "all_terminal": true
    }
  },
  "coverage": {
    "delivery_paths": ["direct_successor"],
    "assessor_cardinality": ["zero"],
    "successor_gating": ["direct_after_handoff"],
    "playbook_shapes": ["custom_role_names", "one_checkpoint"],
    "storage_execution": ["artifact_heavy", "default_image", "cold_specialists", "warm_orchestrators"],
    "configuration_optional_controls": ["approval_prose_only"]
  }
}
