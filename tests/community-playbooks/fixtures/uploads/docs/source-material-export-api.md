# Export API Notes

Current API surface:

- create export job
- fetch export job status
- download export results once ready

Documentation gaps:

- no clear explanation of retry behavior
- failure states are under-described
- operator-visible status mapping is inconsistent across docs and UI
